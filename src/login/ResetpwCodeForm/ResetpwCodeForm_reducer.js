import * as actions from "login/ResetpwCodeForm/ResetpwCodeForm_actions";


const codeResetData = {
  email: "",
};

let codeResetReducer = (state = codeResetData, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default codeResetReducer;
